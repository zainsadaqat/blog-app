<section>
  <% @posts.each do |post| %>
    <%= link_to post_path(@user, post) do %>
      <div>
        <h2><%= post.title %></h2>
        <p><%= post.text %></p>
      </div>
    <% end %>
  <% end %>
  <%= link_to "Create a new post", new_post_path %>
  <%= link_to "Back", root_path %>
</section>
