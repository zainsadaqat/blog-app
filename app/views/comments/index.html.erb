<section>
  <h2>Comments</h2>
  <% @post.comments.each do |comment| %>
    <p><%= comment.text %></p>
  <% end %>
  <%= link_to "Post comment", new_comment_path %>
  <%= link_to "Back", posts_path %>
</section>
